{
  "name": "shri2025-performance",
  "version": "1.0.0",
  "description": "Optimized performance version for SHRI 2025",
  "main": "index.html",
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --minify --outfile=dist/bundle.js --format=iife --tree-shaking=true --drop:console --drop:debugger",
    "dev": "esbuild src/main.jsx --bundle --outfile=dist/bundle.js --format=iife --watch"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  }
}